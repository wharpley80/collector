<?php

namespace Tests\Feature;

use Illuminate\Foundation\Testing\RefreshDatabase;
use App\User;
use App\UserGame;
use Tests\TestCase;

class SegaTest extends TestCase
{
    use RefreshDatabase;

        /**
     * This is a test to verify that a user can add a game to their collection.
     *
     * @return void
     */
    public function testSortReturnsKeyValue()
    {
/*
        $mySort = $sort ?? 'name-asc';
        $sortParams = explode('-', $mySort);

        return $sortParams;
$data = 'name-asc';

$response = $this->post('/single-game', $data);
        
$this->assertCount(1, UserGame::all());
*/
    }
}